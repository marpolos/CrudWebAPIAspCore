/* using CrudWebAPIAspCore.Models;

namespace CrudWebAPIAspCore.Helper;

public class Helper
{
    public FilmeOutputModel ToOutputModel(Filme model)
    {
        return new FilmeOutputModel
        {
            Id = model.Id,
            Titulo = model.Titulo,
            AnoLancamento = model.AnoLancamento,
            Resumo = model.Resumo,
            UltimoAcesso = DateTime.Now
        };
    }
} */